CREATE TABLE Sells (
	id INTEGER PRIMARY KEY AUTOINCREMENT,
	date INTEGER NOT NULL,
	totalLiters NUMERIC NOT NULL,
	Amount NUMERIC NOT NULL,
	exchangeRate NUMERIC NOT NULL
);

CREATE TABLE SellsDetails (
	id INTEGER PRIMARY KEY AUTOINCREMENT,
	sellID INTEGER NOT NULL,
	quantity INTEGER NOT NULL,
	presentation NUMERIC NOT NULL,
	unitaryPrice NUMERIC NOT NULL,
	FOREIGN KEY (sellID) REFERENCES Sells (id)
);

CREATE TABLE Bills (
	id INTEGER PRIMARY KEY AUTOINCREMENT,
	date INTEGER NOT NULL,
	type TEXT NOT NULL,
	amount NUMERIC NOT NULL,
	supplier TEXT NOT NULL,
	exchangeRate NUMERIC NOT NULL
);

CREATE TABLE BillsDetails (
	id INTEGER PRIMARY KEY AUTOINCREMENT,
	billID INTEGER NOT NULL,
	description TEXT NOT NULL,
	quantity NUMERIC NOT NULL,
	amount NUMERIC NOT NULL,
	FOREIGN KEY (billID) REFERENCES Bills (id)
);

CREATE TABLE IncomeBs (
	id INTEGER PRIMARY KEY AUTOINCREMENT,
	sellID INTEGER NOT NULL,
	amount NUMERIC NOT NULL,
	FOREIGN KEY (sellID) REFERENCES Sells (id)
);

CREATE TABLE IncomeDollars (
	id INTEGER PRIMARY KEY AUTOINCREMENT,
	sellID INTEGER NOT NULL,
	amount NUMERIC NOT NULL,
	FOREIGN KEY (sellID) REFERENCES Sells (id)
);

CREATE TABLE WithdrawalBs (
	id INTEGER PRIMARY KEY AUTOINCREMENT,
	sellID INTEGER NOT NULL,
	amount NUMERIC NOT NULL,
	FOREIGN KEY (sellID) REFERENCES Sells (id)
);

CREATE TABLE WithdrawalDollars (
	id INTEGER PRIMARY KEY AUTOINCREMENT,
	sellID INTEGER NOT NULL,
	amount NUMERIC NOT NULL,
	FOREIGN KEY (sellID) REFERENCES Sells (id)
);

CREATE TABLE IncomeBankBs (
	id INTEGER PRIMARY KEY AUTOINCREMENT,
	sellID INTEGER NOT NULL,
	amount NUMERIC NOT NULL,
	FOREIGN KEY (sellID) REFERENCES Sells (id)
);

CREATE TABLE WithdrawalBankBs (
	id INTEGER PRIMARY KEY AUTOINCREMENT,
	sellID INTEGER NOT NULL,
	amount NUMERIC NOT NULL,
	FOREIGN KEY (sellID) REFERENCES Sells (id)
);

CREATE TABLE Inventory (
	id INTEGER PRIMARY KEY AUTOINCREMENT,
	description TEXT NOT NULL,
	stock INTEGER NOT NULL,
	unitaryPrice NUMERIC NOT NULL	
);

CREATE TABLE SellsItems (
	id INTEGER PRIMARY KEY AUTOINCREMENT,
	date INTEGER NOT NULL,
	amount NUMERIC NOT NULL,
	exchangeRate NUMERIC NOT NULL	
);

CREATE TABLE SellItemsDetails (
	id INTEGER PRIMARY KEY AUTOINCREMENT,
	idSell INTEGER NOT NULL,
	idItem INTEGER NOT NULL,
	quantity INTEGER NOT NULL,
	FOREIGN KEY (idSell) REFERENCES SellsItems (id),
	FOREIGN KEY (idItem) REFERENCES Inventory (id)
);

CREATE TABLE IncomeBsSellsItems (
	id INTEGER PRIMARY KEY AUTOINCREMENT,
	idSell INTEGER NOT NULL,
	amount NUMERIC NOT NULL,
	FOREIGN KEY (idSell) REFERENCES SellsItems (id)
);

CREATE TABLE IncomeBankBsSellsItems (
	id INTEGER PRIMARY KEY AUTOINCREMENT,
	idSell INTEGER NOT NULL,
	amount NUMERIC NOT NULL,
	FOREIGN KEY (idSell) REFERENCES SellsItems (id)
);

CREATE TABLE IncomeDollarsSellsItems (
	id INTEGER PRIMARY KEY AUTOINCREMENT,
	idSell INTEGER NOT NULL,
	amount NUMERIC NOT NULL,
	FOREIGN KEY (idSell) REFERENCES SellsItems (id)
);

CREATE TABLE WithdrawalBankBsSellsItems (
	id INTEGER PRIMARY KEY AUTOINCREMENT,
	idSell INTEGER NOT NULL,
	amount NUMERIC NOT NULL,
	FOREIGN KEY (idSell) REFERENCES SellsItems (id)
);

CREATE TABLE WithdrawalBsSellsItems (
	id INTEGER PRIMARY KEY AUTOINCREMENT,
	idSell INTEGER NOT NULL,
	amount NUMERIC NOT NULL,
	FOREIGN KEY (idSell) REFERENCES SellsItems (id)
);

CREATE TABLE WithdrawalDollarsSellsItems (
	id INTEGER PRIMARY KEY AUTOINCREMENT,
	idSell INTEGER NOT NULL,
	amount NUMERIC NOT NULL,
	FOREIGN KEY (idSell) REFERENCES SellsItems (id)
);